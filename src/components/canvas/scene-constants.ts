import {
  DIST_MULT,
  METER,
  SIMULATION_RADIUS,
  SOLAR_SYSTEM_RADIUS,
} from '@/simulation/utils/constants';

export const NEAR_CLIP = 1e-5;
export const FAR_CLIP = 2 * SIMULATION_RADIUS;

export const SURFACE_NEAR_CLIP = METER / 10;

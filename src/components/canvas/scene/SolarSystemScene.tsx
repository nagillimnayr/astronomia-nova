import { SolarSystem } from '@/components/canvas/solar-system/SolarSystem';
import CanvasWrapper from './CanvasWrapper';

const SolarSystemScene = () => {
  return (
    <CanvasWrapper>
      <SolarSystem />
    </CanvasWrapper>
  );
};

export default SolarSystemScene;
